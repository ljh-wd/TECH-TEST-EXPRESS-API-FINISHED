{
  "name": "tech-test-express-api",
  "private": true,
  "version": "1.0.0",
  "description": "One half of 3D Group's technical assessment. An express api using TypeScript.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "nodemon",
    "tsc": "./node_modules/typescript/bin/tsc",
    "build": "npm run tsc --noEmitOnError",
    "start": "npm run build && node dist/index.js",
    "typecheck": "npm run tsc --noEmit"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.7",
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": "^20.12.2"
  }
}
